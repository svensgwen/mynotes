# React JS Research
- React JS is a Javascript library, they do not return HTML markup, The return JSX which is kind of javascript code.
- React JS allows us to use dynamic values, using curly braces "{}".


# React JS parts
- Components
- Props



## Properties
- Dynamic values in curly braces accept number strings, which can be used to make attributes dynamic and style components using js object within curly braces.

## Rendering
- Converting of a react component into equivalent DOM
- Json Object converts this?
- Json Object has Type, Props, Key and refs

## Corresponding react element
```
{
    type: 'h1',
    props:{
        name: 'Bob',
    }
}
```

## Components
- Building blocks of react app
```jsx
<Header />
<Content />
<Footer />
```
These components can be used as many times as we want